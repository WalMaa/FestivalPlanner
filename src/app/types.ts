export interface Festival {
    id: string;
    name: string;
    startDate: string;
    endDate: string;
    location: string;
}
 
export interface Artist {
    id: string;
    name: string;
    spotifyId: string;
  }